use super::{driver_name::DriverName, driver_statistics::DriverStatistic};

pub struct Driver {
    pub name: DriverName,
    pub statistics: DriverStatistic,
}

impl Driver {
    fn new() {
        
    }
}